import {
    StyleSheet,
} from 'react-native';

import {
    windowWidth,
    windowHeight,
} from '/styles';

export default StyleSheet.create({
    container: {
        width: windowWidth,
        height: windowHeight,
    },
});
