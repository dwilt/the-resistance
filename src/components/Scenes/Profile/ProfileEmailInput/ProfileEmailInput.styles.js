import {
    StyleSheet,
} from 'react-native';

import {
    gutter,
    white,
} from '/styles';

export default StyleSheet.create({
    text: {
        padding: gutter,
        backgroundColor: white,
        marginBottom: gutter,
    },
});
