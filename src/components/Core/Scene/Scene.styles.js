import {
    StyleSheet,
} from 'react-native';

import {
    lightBlue,
} from '/styles';

export default StyleSheet.create({
    container: {
        backgroundColor: lightBlue,
        flex: 1,
    },
    contentContainer: {
        flex: 1,
        flexDirection:`row`,
    },
});
