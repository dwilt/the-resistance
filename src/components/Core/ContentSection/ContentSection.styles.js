import {
    StyleSheet,
} from 'react-native';

import {
    white,
} from '/styles';

export default StyleSheet.create({
    container: {
        flex: 1,
        backgroundColor: white,
    },
});
