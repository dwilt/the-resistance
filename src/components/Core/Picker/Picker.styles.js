import {
    StyleSheet,
} from 'react-native';

import {
    gutter,
    white,
} from '/styles';

export default StyleSheet.create({
    container: {
        backgroundColor: white,
        marginBottom: gutter,
    },
});
