import { StyleSheet } from "react-native";

import { gutter } from "/styles";

export default StyleSheet.create({
    error: {
        padding: gutter,
        textAlign: `center`,
        color: `red`
    }
});
