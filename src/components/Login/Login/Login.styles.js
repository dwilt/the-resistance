import { StyleSheet } from 'react-native';

import { gutter } from 'styles';

export default StyleSheet.create({
    container: {
        paddingTop: gutter * 2,
        paddingLeft: gutter,
        paddingRight: gutter,
    },
});
